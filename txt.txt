mkdir -p {core,db/migrations,modules/{auth,users,workouts,meals,admin},worker}

# 2. Move into the root project directory
cd eu_app_backend

# 3. Create the loose files
touch requirements.txt main.py db/database.py

# 4. Create the nested files using brace expansion
touch core/{config,security,exceptions}.py
touch modules/workouts/{router,service,logic,repository,schemas,models}.py
touch worker/{celery_app,tasks}.py

# Optional: Create __init__.py files to ensure Python treats these folders as modules
find app -type d -exec touch {}/__init__.py \;